import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.ekstep.djp',
  appName: 'Khel Khel Mein',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
